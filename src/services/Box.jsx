import styled from "@emotion/styled";
import { color, space, layout, typography, grid, flexbox, shadow, border } from 'styled-system'

const Box = styled('div')(color, space, layout, typography, grid, flexbox, shadow, border);

export default Box